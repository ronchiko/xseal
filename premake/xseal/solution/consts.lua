local PROJECT_KIND = {
    Console = "ConsoleApp",
    Library = "StaticLib"
}

local PROJECT_CONFIGURATION = {
    Debug = "Debug",
    Release = "Release"
}

return PROJECT_CONFIGURATION, PROJECT_KIND
